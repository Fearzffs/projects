---
description: Portfolio project backlog order
alwaysApply: true
---

# Backlog

- Canonical order: [`docs/BACKLOG.md`](docs/BACKLOG.md).
- Portfolio spine is complete through `12-showcase-demo`.
- Do not invent new portfolio folders or reorder the backlog unless the human asks.
- Do not retrofit logger into `01`–`04`; keep primitives clean.
- Post-demo default: harden / theme existing `01`–`12` (see LEARNING Deep track), not new sample folders.

# Morning learning ritual

- At session start (learning day): one **interview round slice** from [`docs/LEARNING.md`](docs/LEARNING.md) daily cycle (Language → Concurrency → Coding → Design → Project deep-dive). 15–30 min.
- Not labeled bank quizzes. After the slice, map misses to the gap table and update scores.
- Next default: **C++ language** (step 1). 2026-09-02 completed the five-slice interview loop (`05` deep-dive last).
- Deep/lab (`08` TSAN) only if scheduled / asked.
