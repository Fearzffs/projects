---
description: Portfolio project backlog order
alwaysApply: true
---

# Backlog

- Canonical order: [`docs/BACKLOG.md`](docs/BACKLOG.md).
- Portfolio spine is complete through `12-showcase-demo`.
- Do not invent new portfolio folders or reorder the backlog unless the human asks.
- Do not retrofit logger into `01`–`04`; keep primitives clean.
- Post-demo default: harden / theme existing `01`–`12` (see LEARNING Deep track), not new sample folders.

# Morning learning ritual

- At session start (when the human is doing a learning day): one **interview snippet** from [`docs/LEARNING.md`](docs/LEARNING.md) (Interview queue) — not labeled bank A1/C4 quizzes.
- Banks A–F in LEARNING are **answer keys**. After they answer, map the snippet to a gap and update scores.
- First weeks: remaining 3s (B flag vs count, C `lock()`, D move-as-cast, E virtual vs override / slicing), plus A smoke for a second cold 4.
- 1–2 snippets; 15–30 minutes total. Then deep/lab only if scheduled (TSAN/themes next week unless asked).
